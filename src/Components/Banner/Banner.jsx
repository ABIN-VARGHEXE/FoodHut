import React from "react";

const Banner = () => {
  return (
    <div>
      <h1>
        <span>{`It’s Now `}</span>
        <span className="word-colour-1">More Easy</span>
        <span>{` to `}</span>
        <span className="word-colour-2">Order</span>
        <span>{` by Our Mobile `}</span>
        <span className="word-colour-1">App</span>
      </h1>
      <p>
        All you need to do is downlode one of the best delivery apps, make a and
        most companies are opting for mobile app devlopment for food delivery
      </p>
    </div>
  );
};

export default Banner;
